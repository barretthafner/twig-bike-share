<% include ../../partials/header %>
<% include ../../partials/nav %>

<div class="container">

	<h2 class="row justify-content-center mt-2">Edit Bike #<%= bike.bikeId %></h2>

	<div class="row justify-content-center">
		<form class="col-md-6" action="<%= routes.bikes %>/<%= bike.id%>" method="POST">
			<div class="form-group row">
				<label for="bikeId">Bike Id</label>
				<input class="form-control" id="bikeId" name="bike[bikeId]" type="text" value="<%= bike.bikeId %>">
			</div>
			<div class="form-group row">
				<label for="lockCode">Lock Code</label>
				<input class="form-control" id="lockCode" name="bike[code]" type="text" value="<%= bike.code %>">
			</div>
			<div class="form-group row my-2 justify-content-center">
				<button class="col-md-4 btn btn-lg btn-primary btn-block">Submit!</button>
				<input type="hidden" name="_method" value="PUT">
			</div>
		</form>
	</div>

	<form
		class="row justify-content-center"
		action="<%= routes.bikes %>/<%= bike.id %>"
		method="POST"
		onsubmit="return confirm('Do you really want to delete bike #<%= bike.bikeId %>?');">

		<button class="btn btn-lg btn-danger">Delete Bike</button>
		<input type="hidden" name="_method" value="DELETE">
	</form>

	<div class="row justify-content-center my-2">
		<a class="btn btn-lg btn-success" href="<%= routes.bikes %>">Go Back</a>
	</div>

</div>

<% include ../../partials/scripts %>
<% include ../../partials/footer %>
